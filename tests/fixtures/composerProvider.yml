-
  input:
    file: 'composer1.json'
    artifact: 'dependency-1-{pretty-version}.tar.gz'
    type: 'tar'
    command: 'install'
  output:
    existing:
      - /vendor/openeuropa/dependency-1/composer.json
      - /vendor/openeuropa/dependency-2/composer.json
      - /vendor/openeuropa/dependency-1/artifact.txt
-
  input:
    file: 'composer2.json'
    artifact: 'dependency-1-{pretty-version}.zip'
    type: 'zip'
    command: 'install'
  output:
    existing:
      - /vendor/openeuropa/dependency-1/composer.json
      - /vendor/openeuropa/dependency-2/composer.json
      - /vendor/openeuropa/dependency-1/artifact.txt
-
  input:
    file: 'composer1.json'
    artifact: 'dependency-1-{pretty-version}.tar.gz'
    type: 'tar'
    command: 'install --prefer-source'
  output:
    existing:
      - /vendor/openeuropa/dependency-1/composer.json
      - /vendor/openeuropa/dependency-2/composer.json
      # @todo: The following file is not supposed to exist. We need a follow up ticket to fix this.
      - /vendor/openeuropa/dependency-1/artifact.txt
